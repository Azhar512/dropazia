# ✅ Correct Path to Connection String

## ❌ You're Currently On: Project Settings (General)
This is NOT where the connection string is!

## ✅ Where to Go Instead

### Method 1: From Left Sidebar
1. Look at the **left sidebar**
2. Find **"Database"** (it's in the main navigation, NOT under Project Settings)
3. Click **"Database"**
4. You'll see options like: Schema Visualizer, Tables, Functions, etc.
5. Click **"Settings"** (under the "Configuration" section within Database)
6. Scroll down to find **"Connection string"** section

### Method 2: From Top Settings
1. Click the **gear icon** (⚙️) at the TOP of the left sidebar (above "Project Overview")
2. This is the **top-level Settings**
3. Click **"Database"** in the Settings menu
4. Scroll down to **"Connection string"** section
5. Click the **"URI"** tab

## 🎯 Visual Guide

**Current location:**
- Left sidebar → Project Settings → General ❌

**Go to:**
- Left sidebar → **Database** → **Configuration** → **Settings** ✅
- OR
- Top gear icon ⚙️ → **Database** → Scroll to "Connection string" ✅

## 📝 Quick Steps

1. **Click "Database"** in the left sidebar (main navigation)
2. You'll see Database sections
3. Under **"Configuration"**, click **"Settings"**
4. Scroll down past "Database password", "Connection pooling", "SSL Configuration"
5. Find **"Connection string"** section
6. Click **"URI"** tab
7. Copy the connection string

## 💡 Alternative: Use Connection Pooling

If you still can't find it, in the same Database Settings page:
- Look for **"Connection pooling"** section
- Use the connection string from there (it will work too!)

